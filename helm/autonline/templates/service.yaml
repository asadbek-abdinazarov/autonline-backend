apiVersion: v1
kind: Service
metadata:
  name: {{ include "autonline.fullname" . }}
  labels:
    {{- include "autonline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autonline.selectorLabels" . | nindent 4 }}
