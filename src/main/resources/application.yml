spring:
  profiles:
    active: prod
springdoc:
  swagger-ui:
    persistAuthorization: true
management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, prometheus
  jpa:
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Tashkent